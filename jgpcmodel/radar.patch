*** ../latest/jgpcmodel/radar.cpp	2011-07-01 17:42:32.000000000 -0400
--- radar.cpp	2011-07-03 08:35:05.489036879 -0400
***************
*** 226,234 ****
      bool ltest = true;
      for (int i = 0; i < 4; i+= 2) {
        // test the points on the segment
!       if (min(vtx[i].px,vtx[i+1].px) > a || max(vtx[i].px,vtx[i+1].px) < a)
  	ltest = false;
!       if (min(vtx[i].py,vtx[i+1].py) > b || max(vtx[i].py,vtx[i+1].py) < b)
  	ltest = false;
      }
      if (ltest) {
--- 226,234 ----
      bool ltest = true;
      for (int i = 0; i < 4; i+= 2) {
        // test the points on the segment
!       if (min(vtx[i].px,vtx[i+1].px) >= a || max(vtx[i].px,vtx[i+1].px) <= a)
  	ltest = false;
!       if (min(vtx[i].py,vtx[i+1].py) >= b || max(vtx[i].py,vtx[i+1].py) <= b)
  	ltest = false;
      }
      if (ltest) {
